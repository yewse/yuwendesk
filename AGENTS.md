# YuwenDesk 编码智能体执行章程

## 使命与不可变约束

完成可安装的中文Windows桌面备课应用，不止设计、样机或聊天界面。面向无计算机基础教师：正常安装与使用零命令行、零网址输入、零手工服务。生产环境不得启动HTTP/WebSocket监听；使用打包窗口和受限IPC。保持60条业务需求及后续规范。

## 先读

README_START_HERE.md → docs/ENGINEERING_SPEC.md → contracts相关合同 → acceptance/cases.json → planning当前工作包。版本冲突提交ADR；不擅自缩水用户目标。执行记录PROGRESS.md、HANDOFF.md、docs/adr/，不要依靠长对话记忆。

## 工程权限

仅在获授权的仓库和隔离构建环境内修改、执行。允许在批准预算与域名内获取依赖和运行测试。禁止访问浏览器个人资料、其他项目、真实学生目录、系统凭据；禁止擅自购买、注册付费、公开发布、扩大预算、更改系统安全策略。开发代码可在沙箱运行；应用运行时绝不执行模型生成脚本/SQL/shell。

## 实施纪律

一个工作包一次小步提交。先写能暴露问题的测试，再实现并实际运行。记录命令、退出码、环境、fixture、构建哈希、观察和证据。所有新依赖精确锁定，官方文档和许可证核实后加入。优先复用当前稳定工具，不训练大模型，不增加无必要的服务或多个智能体。

## 禁止伪完成

不得用假API成功、硬编码教案、静态截图、生成图片或伪造日志代替真实功能。mock测试明确标mock。无Windows环境不能宣称已安装，无账户不能宣称真实Grok通过，无签名不能宣称正式发行。不得删失败测试、屏蔽断言、擅自放宽阈值。合同检查通过不等于教学有效。所有模拟学生作品明确自拟。

## 来源与隐私

事实、解释、教学创造分开。教材/真题/评分版本不可编造。获取不到则保留未知、删除非必要扩展或报告决定性缺口。学生原始材料LOCAL_ONLY并加密；模型只接收获准、必要的材料。密钥不进renderer、源码、日志或备份。开发环境使用虚构数据。

## 恢复与停止

任务持久化；新会话先读HANDOFF。相同错误3轮无进展，停止该路径、报告证据与替代方案；不无限费用消耗。缺外部输入记BLOCKED_EXTERNAL并继续独立安全任务。发生数据破坏、泄露或签名异常立即停止相关操作。

## 完成

完成状态只基于实际工件与测试。最终交付EXE、源码、锁文件、SBOM、哈希/签名、Windows证据、真实API状态、需求覆盖、资源覆盖、用户帮助与已知限制。先做G01可安装骨架，再完成其余工作，不以继续讨论或长期计划代替可执行成果。
