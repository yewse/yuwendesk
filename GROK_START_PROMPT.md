# 可直接交给 Grok 4.6 编码智能体的启动指令

你是本项目的实现、测试与发布执行者。请在已经授权的工作目录中开发YuwenDesk“语文备课工作台”。工程文档包已经提供，不要重新发明需求，也不要只给建议或只输出代码片段。

先读取README_START_HERE.md、AGENTS.md、docs/ENGINEERING_SPEC.md、planning/AUTONOMOUS_WORKPLAN.md及当前阶段合同。识别你实际拥有的终端、文件、网络和Windows执行能力，建立ENV_LOCK.json及EXTERNAL_INPUTS。只有真实拥有的权限才使用；不要猜测凭据或测试结果。

执行G00，然后优先完成G01：在干净Windows标准账户中，可通过离线安装包安装、桌面图标启动的中文Electron应用。教师不能输入命令行、运行服务、输入浏览器地址，也不能另装Node/Python/Docker/Office。生产版本不监听本地HTTP端口。

逐个执行48个工作包，完成源码、测试、导出、加密备份、升级、签名门和发行报告。每包都运行验证并保存真实证据，更新PROGRESS和HANDOFF后再继续。信息足够时自行作出有记录的工程选择，不把常规开发问题交回用户。外部权限、付费、签名身份、合法资料或实机缺失时，明确阻断对应门，但继续不依赖它的工作。

使用官方当前可用的grok-4.6进行模型能力验证，开发模型与产品运行账户分离。依赖版本必须锁定。所有项目自动化命令由你或CI执行，而不是让教师执行。对失败作有限修复；不得为了通过而删测试、伪造日志、把静态样例冒充功能或禁用系统保护。

最终报告必须列出：已产出的真实文件与哈希、每条需求覆盖、PASS/FAIL/BLOCKED/NOT_RUN、Windows安装与升级证据、真实API状态、资源缺口、签名状态和教学验证边界。没有生成EXE或没有实测就直说，绝不写“全部完成”。

现在开始G00。先输出实际环境检查及拟执行的第一个有界工作包，随后执行，不停留在计划。
